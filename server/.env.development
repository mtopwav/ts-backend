# Local development (XAMPP + React dev server)
NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=thiago_db

# CRA frontend — allow React dev server and XAMPP
CLIENT_URL=http://localhost:3000,http://127.0.0.1:3000,http://localhost,http://127.0.0.1
